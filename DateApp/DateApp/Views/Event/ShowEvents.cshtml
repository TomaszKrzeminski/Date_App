@model List<Event>
@{
    ViewData["Title"] = "CancelEvent";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


   
    @*<div class="row">
        @foreach (var Event in Model)
        {
            <div class="col-lg-12" style="min-height:100px; ">
                <form asp-action="ShowEvent" method="get">
                    <div class="btn-group">
                        <input type="hidden" name="EventId" value=@Event.EventId>
                        <h4>@Event.EventName    @Event.City    @Event.Date.ToShortDateString()   </h4>
                        <input type="submit" class="btn btn-danger" value="Szczegóły" />
                    </div>
                </form>
            </div>

        }





    </div>*@



    <div class="row">
        <table class="table">
            <thead>
               
            </thead>
            <tbody>

                @for(int i=0; i<Model.Count; i++)
                {

                    <tr>                                                                   

                        <td>@(i+1)</td>
                        <td>@Model[i].EventName </td>
                        <td>@Model[i].City</td>
                        <td>@Model[i].Date.ToShortDateString()</td>
                        <td>@Html.ActionLink("Szczegóły","ShowEvent","Event",new {EventId= Model[i].EventId },new {@class="btn btn-info" })</td>
                       
                    </tr>              


                }
              
            </tbody>
        </table>
    </div>